const Loading = () => {
  return (
    <div>
      <h2>Loading...</h2>
      {/* if we wanted to add a gif in for visual appeal
      <img src={loadingGif} alt="Loading..." /> */}
      {/* src={image} would need propTypes
      Loading.propTypes = {
        image: PropTypes.string.isRequired
      } */}
    </div>
  );
};

export default Loading;
